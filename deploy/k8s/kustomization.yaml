apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml
# secret.example.yaml deliberately excluded — copy it, fill in real values, apply
# separately (or use `kubectl create secret`, see its header comment).
configMapGenerator:
  - name: vault-publisher-caddyfile
    files:
      - Caddyfile
